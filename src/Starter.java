
public class Starter {

	public static void main(String[] args) {
		View v = new View();
        /*Population myPop = new Population(20, true);
        int oldMaximum = 0;
        int repeated = 0;
        
        // Evolve our population until we reach an optimum solution
        int generationCount = 0;
        while (myPop.getFittest().getFitness() < Fitness.getMaxFitness() && repeated < 10) {
            generationCount++;
            if(oldMaximum == myPop.getFittest().getFitness())
            {
            	repeated++;
            }
            else
            {
            	repeated = 0;
            	oldMaximum = myPop.getFittest().getFitness();
            }
            System.out.println("Generation: " + generationCount + " Fittest: " + myPop.getFittest().getFitness());
            myPop = GeneticAlg.evolvePopulation(myPop);
        }
        System.out.println("########");
        System.out.println("Feasible solution found: "+myPop.getFittest().getFitness()+"! Maximum possible is "+Fitness.getMaxFitness());
        System.out.println("First generation of solution: " + (generationCount-repeated));
        System.out.print("Genes: [ ");
        for (int k = 0 ; k<myPop.getFittest().count(); k++)
        {
        	System.out.print(myPop.getFittest().getGene(k) + " , ");
        }
    	System.out.print(" ]");*/
    	
    	//Interfaccia grafica TODO

	}

}
